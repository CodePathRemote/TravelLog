<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/photo"
        android:title="@string/take_photo"
        app:showAsAction="always" />

    <item android:id="@+id/notes"
        android:title="@string/take_note"
        app:showAsAction="always"/>

    <item android:id="@+id/budget"
        android:title="@string/take_budget"
        app:showAsAction="always"/>

    <item android:id="@+id/car_service"
        android:title="@string/take_car_service"
        app:showAsAction="ifRoom"/>
</menu>